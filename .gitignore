# Logs
logs
*.log
npm-debug.log*
firebase-debug.log*
firebase-debug.*.log*

# Firebase cache
.firebase/

# Firebase config — clés de projet, à ne pas committer (voir firebase-config.example.js)
public/firebase-config.js

# Config API backend — URL locale/prod, à ne pas committer (voir api-config.example.js)
public/api-config.js

# Claude Code local settings
.claude/

# Coverage
coverage
.coverage

# Dependencies
node_modules/

# Divers
.env
*.tgz

# Outil veille externe (scripts/veille-externe) — config avec secret + articles réels du foyer,
# jamais committés (voir config.example.json)
scripts/veille-externe/config.json
scripts/veille-externe/articles-a-importer/*.json
scripts/veille-externe/articles-a-importer/traites/*.json
__pycache__/
*.pyc
