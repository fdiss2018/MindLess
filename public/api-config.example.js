// Copier ce fichier en "api-config.js" (ignoré par git) et renseigner l'URL du
// backend (voir backend/.env.example pour la configuration serveur) :
// - développement local : http://localhost:3000
// - production : l'URL du service Cloud Run une fois déployé
export const API_BASE_URL = 'http://localhost:3000';
